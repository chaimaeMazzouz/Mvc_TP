@model IEnumerable<Exercice1_APP_MVC.Models.Ouvrage>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Titre_Ouv)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PU)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nb_Exp)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Titre_Ouv)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PU)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nb_Exp)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Num_Ouv }) |
            @Html.ActionLink("Details", "Details", new { id=item.Num_Ouv }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Num_Ouv })
        </td>
    </tr>
}

</table>
